FOOTBALL_DATA_TOKEN=your_token_from_football-data.org

# Local development (no Docker): SQLite, zero setup.
DATABASE_URL=sqlite:///./local.db

# When running via `docker compose up`, use Postgres instead:
# DATABASE_URL=postgresql+psycopg2://wc:wc@localhost:5432/wc26